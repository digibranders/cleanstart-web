import { HomeTestimonialsInsiders } from "@/components/sections/_shared/HomeTestimonialsInsiders";

/**
 * Teams-page testimonials. Uses the shared "CleanStart Insiders" light-card
 * layout populated with the home-page customer testimonial data — same
 * content as the /community page.
 */
export function TeamsInsiders() {
  return <HomeTestimonialsInsiders />;
}
